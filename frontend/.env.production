# ==============================================================================
# ENV PROD — Frontend Vercel (https://frontend-snowy-two-23.vercel.app)
#            -> Backend Render (https://thinktuning.onrender.com)
# Chargé automatiquement par Vite lors de `vite build` (npm run build).
# Contient uniquement des URLs publiques : peut être commité.
# ==============================================================================

# Base de l'API FastAPI déployée sur Render (backend, HTTPS obligatoire :
# un frontend https:// ne peut pas appeler du http:// — mixed content bloqué).
VITE_API_URL=https://thinktuning.onrender.com
