# torch : installer d'abord la version CPU avec :
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# puis installer le reste avec : pip install -r requirements.txt
torch>=2.0.0
transformers==5.15.0
datasets>=2.19.0
scikit-learn>=1.4.0
matplotlib>=3.7.0
nltk==3.10.3
nlpaug>=1.1.11
pandas>=2.0.0
numpy==2.4.5
accelerate>=0.30.0
sentencepiece>=0.1.99
peft>=0.11.0
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
pydantic>=2.0.0
prometheus-client>=0.20.0
rich>=13.0.0
pytest>=9.0.0
# starlette : 1.3.1 == fix max des CVE rapportées par pip-audit (PYSEC-2026-248/249,
# 2280/2281). Compatible fastapi>=0.110.0 (Requires-Dist starlette>=0.46.0, sans borne haute).
starlette==1.3.1
pyarrow>=14.0.0
requests>=2.32.0
httpx>=0.27.0
# Agent IA : driver PostgreSQL pour l'outil postgres_query (ia/tools/database_tools.py)
psycopg2-binary>=2.9.9
# Serveur WSGI/ASGI de production (Dockerfile, Phase 5) : gunicorn supervise
# des workers uvicorn (redémarrage worker, max-requests anti-fuite mémoire).
gunicorn>=22.0.0
# Pipeline end-to-end : chargement du fichier de configuration --config (pipeline.py)
pyyaml>=6.0
# Planification d'entraînements récurrents (POST /train/schedule, SCRUM-34)
apscheduler>=3.10.0
# Configuration centralisée typée (app/config/settings.py, pydantic v2)
pydantic-settings>=2.0.0
# P2 Lot A — autorisation agentique : PDP Casbin embarquée (par process, sans
# service distant). Port domaine : app/domain/ports/authorization_ports.py ;
# adaptateur : app/infrastructure/security/authz/casbin_pdp.py. Activée par le
# flag AGENT_SECURITY_AUTHZ_CASBIN (désactivée par défaut, rollback = unset).
casbin>=1.36.0
# Export ONNX (Phase 3) : conversion PyTorch -> ONNX + runtime CPU. Le chemin
# PyTorch reste le défaut ; ONNX est une optimisation additive optionnelle.
# L'export utilise l'exporter legacy torch.onnx.export(..., dynamo=False) ;
# onnxscript (exigé uniquement par l'exporter dynamo de torch) n'est PAS
# nécessaire ici — et aucune version >= 0.12.0 n'existe sur PyPI (max : 0.7.x).
onnx>=1.14.0
onnxruntime>=1.17.0
pymongo>=4.8.0
mongomock>=4.1.2
# P2 lot 15/16 — chiffrement au repos des stores sessions/audit (Fernet,
# core/store_crypto.py) + backups chiffrés (scripts/backup_encrypted.py).
cryptography>=42.0.0