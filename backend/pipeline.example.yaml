# Pipeline end-to-end (pipeline.py) — CLI > YAML > défauts
labeling:
  # input_path: data/unlabeled.csv        # requis (ou --input en CLI)
  # labeled_output: runs/labeled.jsonl
  # model_path: null                       # null = dernière version experiments/models
  text_column: text
  min_confidence: 0.7
  label_batch_size: 32

finetune:
  # output_dir: runs/lora_model            # requis (ou --output_dir en CLI)
  # base_model: TinyLlama/TinyLlama-1.1B-Chat-v1.0
  # validation_file: null
  # epochs: 3
  # finetune_batch_size: 2
  # gradient_accumulation_steps: 8
  # learning_rate: 0.0002
  # max_seq_length: 512
  # lora_r: 16
  # lora_alpha: 32
  # lora_dropout: 0.05
  # target_modules: q_proj,k_proj,v_proj,o_proj,gate_proj,up_proj,down_proj
  use_qlora: true
  seed: 42
