[pytest]
# Only collect tests from the tests/ directory by default.
testpaths = tests

# Make the project root importable (equivalent to PYTHONPATH=.) for every
# pytest run — requires pytest >= 7.0 (project uses pytest 9.x).
# NOTE: rootdir is implicitly set to the directory containing this file.
pythonpath = .
